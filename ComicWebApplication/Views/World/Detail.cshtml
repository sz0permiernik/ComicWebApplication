@model World
@{
    ViewData["Title"] = "Worlds - ";
}

<div class="container mx-auto">
    <div class="container mb-5">
        <div class="flex rounded overflow-hidden shadow-md p-4 mt-5" style="background-color:rebeccapurple">
            <div style="float: left; margin-right: 50px">
                <img alt="World Image"
                     height="400"
                     src=@Model.ImageURL
                     class="aspect-w-3 aspect-h-4 object-cover"
                     width="300" />
            </div>
            <div>
                <div class="dropdownSpace">
                    <h1>@Model.Name</h1>
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown">
                        </button>
                        <ul class="dropdown-menu text-center">
                            @if (User.Identity.IsAuthenticated)
                            {
                                <a class="btn btn-dark btn-sm" asp-area="" asp-controller="World" asp-action="Edit" asp-route-id="@Model.WorldID">Edytuj</a>
                                <a class="btn btn-dark btn-sm" asp-area="" asp-controller="World" asp-action="Delete" asp-route-id="@Model.WorldID">Usuń</a>
                            }
                        </ul>
                    </div>
                </div>
                <h5 class="text font-bold text-white mt-lg-5 mb-3">Szczegóły świata</h5>
                <p class="text-white">Opis świata: @Model.Description</p>
            </div>
        </div>
    </div>
</div>